// Rotating background images for the landing page (index.html) specifically - a separate
// pool from app-backgrounds.js, since this one IS the whole point of that page rather than
// staying subtle behind real content. Add filenames here once you've dropped images into
// /landing-bg/ - empty is a safe default, the page just shows its plain solid background
// color until you add some.
var LANDING_BACKGROUNDS = [
  // "landing-bg/landing-01.jpg",
];
