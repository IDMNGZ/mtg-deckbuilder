// Rotating background images shown behind the app's own tabs (app.html), picked randomly
// on each load. Add filenames here once you've dropped images into /app-bg/ - empty is a
// safe default, the background just stays the plain solid color until you add some.
var APP_BACKGROUNDS = [
  // "app-bg/app-bg-01.jpg",
];
