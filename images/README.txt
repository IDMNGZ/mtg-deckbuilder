One-off app images that aren't part of the random landing-page pool go here
(e.g. the About page's Bio photo).

Drop a file in and tell Claude the filename - it needs one small edit in
app.html (the About tab's photo element) to actually start using it, so it
won't appear automatically just by being in this folder.
