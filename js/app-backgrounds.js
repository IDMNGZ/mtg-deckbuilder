// Pool of app-wide background images (behind Browse/Collection/Deck Builder/etc, not the
// landing page - see js/landing-backgrounds.js for that one). One is picked at random every
// time the app loads. Add new files to /app-bg/ and list their filenames here.
var APP_BACKGROUNDS = [
];
